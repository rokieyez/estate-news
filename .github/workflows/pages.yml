name: 사이트 배포

# output/ 이 바뀌면 휴대폰용 사이트를 다시 만들어 GitHub Pages 에 올립니다.
# 최초 1회: 저장소 Settings → Pages → Source 를 "GitHub Actions" 로 바꿔 주세요.
on:
  push:
    # 기본 브랜치에서만 배포합니다. 브랜치 이름을 바꾸면 여기도 고쳐야 합니다.
    branches:
      - main
      - claude/real-estate-blog-automation-gwtgjb
    paths:
      - "output/**"
      - "rebrief/**"
      - "config/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements.txt

      - run: pip install -r requirements.txt

      - name: 사이트 만들기
        run: python -m rebrief site

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - id: deployment
        uses: actions/deploy-pages@v4

      - name: 주소 안내
        run: |
          {
            echo "## 사이트가 올라갔습니다"
            echo
            echo "매일 아침 아래 주소만 열면 됩니다. 휴대폰에 즐겨찾기 해두세요."
            echo
            echo "**${{ steps.deployment.outputs.page_url }}**"
          } >> "$GITHUB_STEP_SUMMARY"
