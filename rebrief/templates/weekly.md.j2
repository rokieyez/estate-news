---
title: "{{ review.title }}"
slug: "{{ review.slug }}"
week: {{ week }}
period: {{ start }} ~ {{ end }}
category: "{{ category }}"
tags: [{% for t in review.tags %}"{{ t }}"{% if not loop.last %}, {% endif %}{% endfor %}]
description: "{{ review.meta_description }}"
---

**이번 주 다섯 줄**

{% for line in review.five_lines %}
{{ loop.index }}. {{ line }}
{% endfor %}

{{ review.body_markdown }}
{% if trades %}

## 이번 주 실거래 (직접 집계)

{{ trades.month }} 신고된 아파트 매매는 **{{ '{:,}'.format(trades.total) }}건**입니다.
{% if trades.total_change %}이번 주에만 {{ '{:+,}'.format(trades.total_change) }}건이 더 신고됐습니다.{% endif %}


| 지역 | 거래 건수 | 평균 거래가 |
| --- | ---: | ---: |
{% for d in trades.districts %}
| {{ d.name }} | {{ d.count }}건 | {{ (d.avg / 100000000)|round(1) }}억 |
{% endfor %}
{% if trades.index %}

주간 가격지수({{ trades['from'] }} → {{ trades.to }})
{% for name, v in trades.index.items() %}
- {{ name }} {{ v.value|round(2) }}{% if v.change is not none %} ({{ '%+.2f'|format(v.change) }}){% endif %}
{% endfor %}
{% endif %}

*국토교통부 실거래가 신고 자료를 날마다 직접 집계한 값입니다. 해제 신고분은 뺐습니다.*
{% endif %}

---

### 다음 주 볼 것

{% for w in review.next_week_watch %}
- {{ w }}
{% endfor %}

---

*{{ disclaimer }}*
